# TODO

- consolidate scrollbars to be rendered on top the content at the end (with being ok overwritting the last column for this).
- new command: push all normal (non-special, non-sticky) windows of current workspace to another workspace. targets: left/right/up/down (relative) or <id> (absolute). e.g. `pw right`, `pw 3`.
- hotkeys V3: a "hotkeys" tab in cofi where the user can bind arbitrary commands+args to system hotkeys. hotkeys V2 only covers the 3 show-modes (windows/command/workspaces).
