#ifndef TAB_METADATA_H
#define TAB_METADATA_H

#include "app_data.h"

const char *tab_display_name(TabMode tab);
const char *tab_active_name(TabMode tab);
const char *tab_log_name(TabMode tab);

#endif /* TAB_METADATA_H */
